<%= form_for(@vocabulary_word) do |f| %>
  <% if @vocabulary_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vocabulary_word.errors.count, "error") %> prohibited this vocabulary_word from being saved:</h2>

      <ul>
      <% @vocabulary_word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vocabulary %><br />
    <%= f.text_field :vocabulary %>
  </div>
  <div class="field">
    <%= f.label :word %><br />
    <%= f.text_field :word %>
  </div>
  <div class="field">
    <%= f.label :known %><br />
    <%= f.check_box :known %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
